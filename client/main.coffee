# Meteor.subscribe('events')

# Meteor.call "getCoursesTable", (error, result)->
#     console.log result

FlashMessages.configure({
    autoHide: true
    hideDelay: 3000
    # autoScroll: true
 })

# Template.layout.helpers(
#     pageTitle: ()->
#         Session.get('pageTitle')
# )
